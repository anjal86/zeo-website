version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    restart: always
    environment:
      - PORT=3000
    # Note: Configure any extra environment variables or database links here
